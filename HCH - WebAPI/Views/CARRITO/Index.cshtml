@model IEnumerable<HCH___WebAPI.Models.CARRITO>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PRODUCTO.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.USUARIO.Nombres)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PRODUCTO.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.USUARIO.Nombres)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdCarrito }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdCarrito }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdCarrito })
        </td>
    </tr>
}

</table>
